# Replace the placeholders with your actual MongoDB URI and JWT secrets
# Ensure to keep your secrets safe and do not expose them in public repositories
# Example values are provided below, but you should replace them with your own secure values.
# Example values (do not use these in production):  
DATABASE_URI=your_mongodb_uri_here
JWT_SECRET=your_jwt_secret_here
JWT_REFRESH_SECRET=your_refresh_secret_here
PORT=your_port

